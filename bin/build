#!/bin/sh

# This is needed to work around the need for trampolining at Heroku
# (to keep down memory usage) and the inability of that to work when
# we have the lein-cljsbuild plugin. I hope this is all just
# temporary, until I understand the tool chain better.

lein cljsbuild once
lein compile :production

